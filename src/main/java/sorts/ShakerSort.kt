package sorts
/*

class ShakerSort<T> : AbstractSort<T>(){
    override fun sort(list: MutableList<in Comparable<T>>, start: Int, end: Int) {
        for (i in start until end){
            var left = start
            var right = end-1
            do{
                for(k in left until right){

                }
            }while (left<right)
        }
    }


}*/
